<!DOCTYPE html>
<html lang="sl">

	<head>
		<!-- meta tags -->
		{% include "meta.twig" %}
		
		<link rel="stylesheet" href="{{alias('@web') }}/style/landing.css">
		<link rel="stylesheet" href="{{alias('@web') }}/style/normal.css">
		<link rel="stylesheet" href="{{alias('@web') }}/style/events.css">
	</head>

	<body>

		<!-- navbar + bg img-->
		<header class="events-bg">
			<div
				class="navbar navbar-parallax" id="fade-out">

				<!-- logo -->
				<h1>
					<a style="display: flex;" href="{{alias('@web') }}/sl/"><img alt="Kino Klub" class="logo" src="{{alias('@web') }}/img/KK-LOGO.png"></a>
				</h1>

				<!-- navbar desktop -->
				<nav class="navbar1-parallax">
					<a class="link hover" href="novice">ZAPISKI IN ZGODBE</a>
					<a class="link hover" href="dogodki">KOLEDAR DOGODKOV</a>
					<a class="link hover" href="projects">PROJEKTI</a>
					<a class="link hover" href="about">O NAS</a>
				</nav>

			</div>
		</header>

		<!-- navbar mobile -->
		<nav id="mobile-meni" class="mobile-meni display-none">

			<a href="novice">ZAPISKI IN ZGODBE</a>
			<a href="dogodki">KOLEDAR DOGODKOV</a>
			<a href="projects">PROJEKTI</a>
			<a href="about">O NAS</a>
			<spn class="mobile-lan">
				<span><a style="display:inline;" href="{{alias('@web') }}/sl/">SL</a>
				</span>/<span>
					<a style="display:inline;" href="{{alias('@web') }}">
					EN</a></span>
			</spn>

		</nav>

		<!-- hamburger menu -->
		<div class="knof" id="knof" onclick="anime()">
			<div id="sprememba" class="zg"></div>
			<div id="spremembaa" class="sr"></div>
			<div id="spremembaaa" class="sp"></div>
		</div>

		<!-- configuration menu desktop-->
		<nav class="config">

			<span>ZAPISKI IN ZGODBE</span>

			<a class="conf-meni-item active" value="ALL" href="?">VSE</a>
			<a class="conf-meni-item" value="NOTES AND STORIES" href="?cat=NOTES AND STORIES">ZAPISKI IN ZGODBE</a>
			<a class="conf-meni-item" value="ARTICLES" href="?cat=ARTICLES">ČLANKI</a>
			<a class="conf-meni-item" value="PODCASTS" href="?cat=PODCASTS">PODCAST-i</a>

		</nav>

		<!-- configuration menu mobile-->
		<select id="mySelect" onchange="onchange=window.location=this.value" class="form-select form-select-lg mx-auto" style="width: 333px;" aria-label="Default select example">

			<option class="conf-meni-option" value="?cat=">VSE</option>
			<option class="conf-meni-option" value="?cat=NOTES AND STORIES">ZAPISKI IN ZGODBE</option>
			<option class="conf-meni-option" value="?cat=ARTICLES">ČLANKI</option>
			<option class="conf-meni-option" value="?cat=PODCASTS">PODCAST-i</option>

		</select>

		{# Create an entry query with the 'section' and 'limit' parameters #}
		{% set myEntryQuery = craft.entries()
    .section('notes')
    .limit(25) %}

		{# Fetch the entries #}
		{% set entries = myEntryQuery.all() %}
		{# Display the entries #}

		<!-- main content -->
		{% set count = 0 %}
			<main class="jumbotron landing-main"> {% for entry in entries %}
				{% set cat = craft.app.request.getParam('cat') %}
				{% if cat == '' or cat == entry.category %}
				{% if entry.category == 'NOTES AND STORIES' %}
				{% set kateg = 'ZAPISKI IN ZGODBE' %}
				{% elseif entry.category == 'ARTICLES' %}
				{% set kateg = 'ČLANKI' %}
				{% elseif entry.category == 'PODCASTS' %}
				{% set kateg = 'PODCAST-i' %}
				{% endif %}

					{% set count = count + 1 %}
					{% set slika = entry.slika.one %}

					{% if (count is odd) %}
						<section class="row {% if count > 5 %}displayNone disc-more{% endif %}">

							<div class="col-xl-8">
								<span class="category mobile-display">{{ kateg }}</span>
								<a href="{{ entry.slug }}">

									<img alt="{{ entry.altText }}" loading="lazy" class="note-img" src="{{ slika.url }}"></a>
							</div>

							<div class="col-xl-4 title">
								<span class="category desktop-display">{{ kateg }}</span>
								<a style="text-decoration:none;" href="{{ entry.slug }}">
								<h3 class="title">
									{{ entry.title }}
								</h3>
							</a>
								<a class="notes-link" href="{{ entry.slug }}"><img alt="Preberi več" src="{{alias('@web') }}/img/Vectorvec.svg">PREBERI VEČ</a>
							</div>

						</section>
					{% endif %}

					{% if (count is even) %}
						<section class="row reverse {% if count > 5 %}displayNone disc-more{% endif %}">

							<div class="col-xl-4 title">
								<span class="category desktop-display">{{ kateg }}</span>
								<a style="text-decoration:none;" href="{{ entry.slug }}">
								<h3 class="title">
									{{ entry.title }}
								</h3>
							</a>
								<a class="notes-link" href="{{ entry.slug }}"><img alt="Preberi več" src="{{alias('@web') }}/img/Vectorvec.svg">PREBERI VEČ</a>
							</div>

							<div class="col-xl-8">
								<span class="category mobile-display">{{ kateg }}</span>

								<a href="{{ entry.slug }}"><img alt="{{ entry.altText }}" loading="lazy" class="note-img" style="float: right" src="{{ slika.url }}"></a>
							</div>

						</section>
					{% endif %}
				{% endif %}
			{% endfor %}

		</main>

		<!-- more button -->
		{% if count > 5 %}
			<div class="discover" onclick="discMore()">
				<img alt="Odpri več" style="width:70px;" src="{{alias('@web') }}/img/plus.svg">ODPRI VEČ
			</div>
		{% endif %}
{% include "footer-sl" %}