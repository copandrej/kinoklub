<!DOCTYPE html>
<html lang="en">

	<head>
		<!-- meta tags -->
		{% include "meta.twig" %}

		<link rel="stylesheet" href="{{alias('@web') }}/style/landing.css?v=2">
		<link rel="stylesheet" href="{{alias('@web') }}/style/normal.css?v=2">
		<link rel="stylesheet" href="{{alias('@web') }}/style/events.css?v=2">
	</head>

	<body>

		<!-- navbar + bg img-->
		<header class="events-bg">
			<div class="navbar navbar-parallax" id="fade-out">

				<!-- logo -->
				<h1>
					<a style="display: flex;" href="{{alias('@web') }}/en/">
							<img alt="Kino Klub" class="logo" src="{{alias('@web') }}/img/KK-LOGO.png">
					</a>
				</h1>

				<!-- navbar desktop -->
				<nav class="navbar1-parallax">
					<a class="link hover" href="projects">Projects</a>
					<a class="link hover" href="notes">Stories & Postcards</a>
					<a class="link hover" href="events">Event Calendar</a>
					<a class="link hover" href="about">About</a>
					<lan class="nav-lan">
						<a href="{{alias('@web') }}">
							SL
						</a>
						/
						<a class="chosen" href="{{alias('@web') }}/en/">
							EN
						</a>
					</lan>
				</nav>

			</div>
		</header>

		<!-- navbar mobile -->
		<nav id="mobile-meni" class="mobile-meni display-none">

			<!--<a href="notes">NOTES AND STORIES</a>-->
			<a href="projects">Projects</a>
			<a href="notes">Stories & Postcards</a>
			<a href="events">Event Calendar</a>
			<a href="about">About</a>
			<spn class="mobile-lan">
				<span>
					<a style="display:inline;" href="{{alias('@web') }}">SL</a>
				</span>/<span>
					<a style="display:inline;" href="{{alias('@web') }}/en/">
						EN</a>
				</span>
			</spn>

		</nav>

		<!-- hamburger menu -->
		<div class="knof" id="knof" onclick="anime()">
			<div id="sprememba" class="zg"></div>
			<div id="spremembaa" class="sr"></div>
			<div id="spremembaaa" class="sp"></div>
		</div>


		<main>
			<h3 style="margin-left:109px; margin-top:101px;">Projects</h3>
			<div class="jumbotron projects">
				<p>Discover some of the projects that we stand by with. The projects are based on the work we do and did in the past and current lives...</p>


				{% set myEntryQuery = craft.entries()
                .section('projects')
                .limit(25) %}
				{# Fetch the entries #}
				{% set entries = myEntryQuery.all() %}
				{# Display the entries #}

				<!-- main content -->
				{% set count = 0 %}
					{% for entry in entries %}
					{% set count = count + 1 %}

					{% set slika = entry.slika.one %}

						<div class="row"> <a href="{{ entry.slug }}">
							<img alt="{{ entry.altText }}" src="{{ slika.url }}" class="event-img">
						</a>
						<a style="text-decoration:none;" href="{{ entry.slug }}">
							<h4>{{ entry.title }}</h4>
						</a>
						<p>{{ entry.opis }}</p>
						<a class="btn-discover r-more" href="{{ entry.slug }}">
							read more
						</a>
					</div>

                    {% endfor %}
				</div>

			</main>

			{% include "footer.twig" %}</body>
	</body>
</html>
